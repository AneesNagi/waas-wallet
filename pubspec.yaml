name: usdc_wallet
description: A USDC wallet app for Base Network
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Removed all potentially problematic packages
  # flutter_svg: ^2.0.9
  # qr_flutter: ^4.1.0
  # flutter_screenutil: ^5.9.0
  # provider: ^6.1.1
  # http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
